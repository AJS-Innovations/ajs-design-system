# Changelog

## Unreleased

- Replace the admin workspace dropdown with the inspected right-side organization overlay; export OrganizationSwitcher with search, metadata, active state, and optional Manage action.

- AppShell member/admin drawer widths, optional mobile brand, and skip-to-content target.
- Mobile sidebar navigation dismisses the drawer, including when resizing to desktop.
- Full /examples playground in the docs workspace with local member/admin workflows.

## 0.1.0

Initial release of the standalone AJS component library.

- Typed React primitives, Radix controls, tables, navigation, and member/admin patterns.
- ESM and CommonJS builds with TypeScript declarations.
- Compiled Tailwind CSS and configurable AJS design tokens.
- Consumer setup instructions and component usage examples.

This release has no production API integration. Some source application states were unavailable during inspection; see the workspace coverage documentation for limits.
